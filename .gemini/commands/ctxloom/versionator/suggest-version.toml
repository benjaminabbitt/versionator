prompt = "# suggest-version\n\nAnalyze repo, suggest next semver.\n\n## Steps\n1. Read VERSION file\n2. Get changes: `git log --oneline $(git describe --tags --abbrev=0 2>/dev/null || echo \"\")..HEAD`\n3. Analyze commits/code\n\n## Output\n```\nCurrent version: X.Y.Z\nCommits since last release: N\nChanges: [list]\nSuggested next version: X.Y.Z\nReasoning: [semver basis]\n```\n\n## Rules\n(See version-bump for semver rules)\n\nPre-1.0: breaking→MINOR, features→PATCH\n\n**Conservative**: uncertain → lower bump"
