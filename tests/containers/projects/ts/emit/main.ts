// Test application that imports generated version module

import { VERSION, MAJOR, MINOR, PATCH } from './version';

console.log(`Version: ${VERSION}`);
console.log(`Major: ${MAJOR}, Minor: ${MINOR}, Patch: ${PATCH}`);
