from setuptools import setup, find_packages

setup(
    name="testapp",
    version="0.0.0",
    description="Test application for versionator patch testing",
    packages=find_packages(),
    python_requires=">=3.8",
)
