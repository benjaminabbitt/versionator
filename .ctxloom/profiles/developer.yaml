description: "Delegated implementer: the full working profile plus escalation discipline — structural/interface decisions go back to the coordinator instead of being decided alone."
parents:
  - default
bundles:
  - ctxloom+git://github.com/ctxloom/ctxloom-default//bundles/agent-roles#fragments/developer-escalation
