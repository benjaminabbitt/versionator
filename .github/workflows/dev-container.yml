name: Dev Container

on:
  push:
    branches: [ main ]
    paths:
      - '.devcontainer/**'
      - 'Dockerfile'
  pull_request:
    paths:
      - '.devcontainer/**'
      - 'Dockerfile'

jobs:
  test-dev-container:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

